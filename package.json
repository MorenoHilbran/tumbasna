{
  "name": "tumbasna-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:dashboard": "npm install --prefix tumbasna-dashboard",
    "install:whatsapp": "npm install --prefix tumbasna-whatsapp",
    "install-all": "npm run install:dashboard && npm run install:whatsapp",
    "dev:dashboard": "npm run dev --prefix tumbasna-dashboard",
    "dev:whatsapp": "npm run dev --prefix tumbasna-whatsapp",
    "dev": "npx concurrently --kill-others \"npm run dev:dashboard\" \"npm run dev:whatsapp\""
  }
}
